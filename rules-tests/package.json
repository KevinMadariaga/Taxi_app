{
  "name": "taxi-app-rules-tests",
  "description": "Pruebas de firestore.rules y storage.rules contra los emuladores",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore,storage --project demo-taxi-rules \"node --test --test-concurrency=1\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.2"
  }
}
