{
  "name": "taxi-app-rules-tests",
  "description": "Pruebas de firestore.rules contra el emulador de Firestore",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-taxi-rules \"node --test --test-concurrency=1\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.2"
  }
}
