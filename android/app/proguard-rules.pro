# Project-specific ProGuard rules for Android release builds.
# Keep this file even when minify is disabled so release config remains complete.

# Flutter embedding and plugins are typically handled by default rules.
# Add custom keep rules here only if you enable minification and detect runtime issues.
